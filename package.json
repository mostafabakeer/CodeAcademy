{
  "name": "drcode",
  "version": "1.0.0",
  "private": true,
  "description": "منصة تعليم البرمجة - DR Code",
  "scripts": {
    "dev": "npm run dev --prefix client",
    "dev:client": "npm run dev --prefix client",
    "build": "npm run build --prefix client",
    "preview": "npm run preview --prefix client",
    "deploy": "supabase db push && supabase functions deploy api --no-verify-jwt && supabase functions deploy backup --no-verify-jwt && supabase functions deploy cleanup --no-verify-jwt"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
